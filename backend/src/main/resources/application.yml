spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/collabtool
    username: amine
    password: amine
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


logging:
  level:
    org:
      springframework:
        web: debug
        security: debug

firebase-service-account: logistics-dec71-firebase-adminsdk-50hnc-4b8fe72aa9.json

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  api-docs.path: /api-docs